#include <iostream>
#include <cstring>
using namespace std;

int main() {
    char str[100];
    cout << "Enter a string: ";
    cin.getline(str, 100);
    int size = strlen(str);
    cout << "Size of the string: " << size << endl;
    return 0;
}
